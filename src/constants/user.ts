/** constants for number of user type */
const USER_TYPE: { [key: string]: number } = {
    SUPERADMIN: 1,
    ADMIN: 2,
    USER: 3,
};

const ROLE : {[key: string]: number} = {
    SUPERADMIN: 1,
    ADMIN: 2,
    BASIC: 3,
};

export { USER_TYPE, ROLE };
